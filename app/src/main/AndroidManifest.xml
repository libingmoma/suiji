<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.suijifront">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuiJiFront">
        <activity android:name=".MyselfActivity"></activity>
        <activity android:name=".TodoEditActivity" />
        <activity android:name=".TodoAddActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".CsContentEditActivity" />
        <activity android:name=".CsContentActivity" />
        <activity android:name=".CsEditActivity" />
        <activity android:name=".CsTableActivity" />
        <activity android:name=".CsAddActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".JitangActivity" />
        <activity android:name=".MessageLogInActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>